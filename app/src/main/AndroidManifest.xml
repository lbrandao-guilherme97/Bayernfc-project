<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.brandaoladm.aci">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AccessActivities.LoginActivity"
            android:label="@string/login_view"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".CalendarActivity"
            android:label="@string/calendar">
        </activity>

        <activity
            android:name=".EventDescriptionActivity"
            android:label="@string/event_description">
        </activity>

        <activity
            android:name=".CreateEventCalendarActivity"
            android:label="@string/create_an_event">

        </activity>

        <activity
            android:name=".AccessActivities.ForgotYourPasswordActivity"
            android:label="@string/forgot_your_password">
        </activity>

        <activity
            android:name=".AccessActivities.SignupActivity"
            android:label="@string/sign_up">
        </activity>

    </application>

</manifest>